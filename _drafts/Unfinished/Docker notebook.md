

Ctrl + p add Ctrl + q       unattach docker

`docker run -it centos:latest /bin/bash`    启动 docker，并用 bash 连接
`docker commit -m "message" -a "author" container_id author/container:v1`
`docker push author/container`
